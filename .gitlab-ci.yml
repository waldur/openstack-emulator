include:
  - project: "waldur/waldur-pipelines"
    file: "/templates/stages.yml"
  - project: "waldur/waldur-pipelines"
    file: "/templates/release/publish-multiarch.yml"

variables:
  COMPONENT_VERSION: "latest"

.python-setup:
  image: registry.hpc.ut.ee/mirror/library/python:3.12-bullseye
  before_script:
    - python -m pip install --upgrade pip
    - pip install -e ".[dev]"

Run linters:
  extends: .python-setup
  stage: test
  script:
    - ruff format --check .
    - ruff check .

Run type checkings:
  extends: .python-setup
  stage: test
  script:
    - mypy emulator

.test-base:
  stage: test
  script:
    - python -m pip install --upgrade pip
    - pip install -e ".[dev]"
    - pytest --cov=emulator --cov-report=xml --cov-report=term-missing
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml

Run tests v3.10:
  extends: .test-base
  image: registry.hpc.ut.ee/mirror/library/python:3.10-bullseye

Run tests v3.11:
  extends: .test-base
  image: registry.hpc.ut.ee/mirror/library/python:3.11-bullseye

Run tests v3.12:
  extends: .test-base
  image: registry.hpc.ut.ee/mirror/library/python:3.12-bullseye

Run tests v3.13:
  extends: .test-base
  image: registry.hpc.ut.ee/mirror/library/python:3.13-bullseye

Publish latest docker image:
  stage: release
  interruptible: true
  image:
    name: quay.io/containers/buildah:latest
    entrypoint: [""]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
  variables:
    BUILDAH_FORMAT: docker
    BUILDAH_ISOLATION: chroot
    DOCKER_HUB_REGISTRY: "docker.io"
    IMAGE_TAG: "opennode/openstack-emulator:latest"
  before_script:
    - echo "[+] Build and publish latest docker image for ${CI_DEFAULT_BRANCH} branch"
    - echo "${WALDUR_DOCKER_HUB_PASSWORD}" | buildah login -u "${WALDUR_DOCKER_HUB_USER}" --password-stdin "${DOCKER_HUB_REGISTRY}"
  script:
    - echo "[+] Building image ${IMAGE_TAG}"
    - |
      buildah bud \
        --build-arg COMMIT_INFO="${CI_COMMIT_SHA}" \
        --build-arg VERSION="latest" \
        -t "${IMAGE_TAG}" \
        -f "${CI_PROJECT_DIR}/Dockerfile" \
        "${CI_PROJECT_DIR}"
    - buildah push "${IMAGE_TAG}" "docker://${IMAGE_TAG}"
